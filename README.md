# nimopt
nimopt – Algebraic optimization modeling with lazy constraint expansion. Built on nimblend for labeled N-dimensional arrays with automatic broadcasting. Define sets, parameters, variables, and constraints with expressive syntax; constraints expand only when writing LP files, enabling instant model construction. Optional Rust acceleration
